const num1 = 7;
const num2 = 10;

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

addValues();
// if there is a function inside a module 
// it will work even if we don't invoke it or assign it to any variable